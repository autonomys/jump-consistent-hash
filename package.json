{
  "name": "@subspace/jump-consistent-hash",
  "description": "Jump consistent hash implementation in TypeScript",
  "keywords": [
    "jump",
    "consistent",
    "hashing"
  ],
  "version": "1.0.0",
  "homepage": "https://github.com/subspace/jump-consistent-hash",
  "author": "Nazar Mokrynskyi <nazar@mokrynskyi.com>",
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && npm run lint && npm run compile",
    "clean": "rm -rf dist",
    "compile": "tsc -b",
    "lint": "tslint --project .",
    "test": "npm run lint && ts-node node_modules/.bin/tape tests/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/subspace/jump-consistent-hash.git"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "devDependencies": {
    "@types/tape": "^4.2.33",
    "tape": "^4.11.0",
    "ts-node": "^8.3.0"
  },
  "engineStrict": true,
  "engines": {
    "npm": ">= 12.0.0"
  }
}
